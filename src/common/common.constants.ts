export const BearerType = 'Bearer';

export enum Status {
  Active = 'Active',
  InActive = 'Inactive',
}

export enum Gender {
  Male = 'Male',
  Female = 'Female',
  Other = 'Other',
}

export enum Role {
  Admin = 'Admin',
  User = 'User',
}

export enum CreateRequest {
  Reject = 'Reject',
  Approve = 'Approve',
  Wait = 'Wait',
}
